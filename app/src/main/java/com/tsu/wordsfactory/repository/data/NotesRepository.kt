package com.tsu.midtermexam3.data

import androidx.lifecycle.LiveData

class NotesRepository(private val WordsDao: WordsDao) {
/*
    val wordsList: LiveData<List<WordModel>> = WordsDao.getAllWords()

    suspend fun addNote(note: WordModel) {
        WordsDao.addWord(note)
    }

    suspend fun deleteNote(text: String, time: String) {
        WordsDao.deleteWord(text, time)
    }
*/

}